#pragma once
#include <fstream>
#include <string>
#include <regex>
#include <variant>
#include <stdexcept>
using namespace std;

variant<int, float, double> extractIFDFromTxt(const string& fileName) {

    
}